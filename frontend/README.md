# Quatava frontend

Next.js (App Router) frontend for Quatava. See the [repo root README](../README.md) for monorepo setup, and [`../DEPLOY.md`](../DEPLOY.md) for the canonical deployment process — this package is not deployed via Vercel.

```bash
pnpm --filter frontend dev
```

Locale-based routes live under `app/[locale]/`.
